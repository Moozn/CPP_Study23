#include <iostream>

using namespace std;

int main()
{
	// 표기법
	 
	// 언더바 표기법
	//int _num; //매개변수로 선언시 많이 사용

	// 파스칼 표기법
	//int PlayerHp;
	
	// 헝가리안 표기법
	//int iNum;
	//float fNum;
	//char chName;
	//bool bNum;

	// 변수 : 데이터를 저장할 메모리를 할당
	// 변수 선언 규칙
	// 변수명에 제일 첫부분은 숫자가 될수없다.
	// 변수명에는 _(언더바)를 제외한 특문이 들어갈 수 없다.
	
	// 자료형
	
	// 정수 자료형
	// short sNum; //2바이트 -32,768 ~ 32,767 
	// int iNum; //4바이트 -2,147,483,648 ~ 2,147,483,647   21억 정도
	// long lNum; //4바이트 
	// long long llNum; //8바이트 -2^64 ~ 2^64;

	// unsigned : 자료형의 음수값을 없앤다
	// unsigned short usNum; // 65535

	// 실수 자료형
	//float fNum = 1.1f; // 4바이트 (7개 자리수) 
	//double dNum = 2.34; // 8바이트 (15개 자리수)

	// 문자 자료형 
	// char chNum = 99; // 1바이트 -127 ~ 127 255. 기본적으로 정수형태로 저장됨
	// string sStr = "asdfghjkl"; // 문자열을 저장
	
	// 불리언
	//bool nNum = true; //1바이트, 참/거짓을 저장 

	//변수의 초기화가 필요한 이유
	//int iNum1;
	//int iNum2 = 12;
	//iNum1 = 11;
    //
	//cout << iNum1 << endl;
	//cout << iNum2 << endl;
	// 초기화 하지 않은 변수에는 쓰레기값이 들어가있어서 오류가 발생한다.

	//-----------------------------
	//상수
	//int iNum = 10 + 20;
	// 10, 20 : 리터럴 상수(임시 메모리) 기본적으로 정수형이면 int로 잡힘
	// 10+20의 값인 30도 임시메모리에 잡히고 그 후에 할당
	// 임시 메모리는 그 라인을 넘어가면 소멸

	//const 키워드
	//const int iNum = 10; // iNum은 읽기만 허용한다. 수정이 불가능하다.
	//const키워드가 붙으면 선언과 동시에 초기화
}
